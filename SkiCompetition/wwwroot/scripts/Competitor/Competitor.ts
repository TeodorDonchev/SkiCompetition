class Competitor {
    //TODO: add all props as knockout observables
    constructor() {
    //TODO: add all values as constructor parameters
    }
}

// The default export returns the component details object to register with KO  
export default Competitor;